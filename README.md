 Customer Segmentation (Clustering)

An unsupervised learning project that groups customers based on purchasing behavior.
Uses algorithms like K-Means Clustering to identify unique customer groups for marketing analysis.
